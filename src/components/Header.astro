<header class="w-full mt-8 fixed top-0 z-50">
    <div class="content-container flex justify-between gap-4 items-center">
        <a href="/">
            <img src="/images/logo.png" alt="ACE" class="h-16 w-auto">
        </a>
    
        <nav class="relative flex justify-between gap-8 items-center">
            <a href="/events" class="nav-link">events</a>
            <a href="/faculty" class="nav-link">faculty</a>
            <a href="/gallery" class="nav-link">gallery</a>
            <a href="/team" class="nav-link">team</a>
            <a href="/developers" class="nav-link">developers</a>
        </nav>
    </div>
</header>

<style>
    nav > a {
        text-transform: capitalize;
        position: relative;
        display: inline-block;
        padding-bottom: 4px;
        overflow: hidden;
    }

    nav > a::after {
        content: '';
        position: absolute;
        left: 0;
        bottom: 0;
        width: 100%;
        height: 3px;
        background: linear-gradient(to right, #00f,rgb(46, 144, 235) );
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.3s ease-out;
        border-radius: 0 0 5px 5px;
    }

    nav > a:hover::after {
        transform: scaleX(1);
    }

    .nav-link {
        transition: transform 0.3s ease-out;
    }

    .nav-link:hover {
        transform: scale(1.1);
        text-shadow: 0 0 5px rgba(255, 255, 255, 0.6);
    }
</style>
